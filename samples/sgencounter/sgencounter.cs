using System;

public class SgenCounter
{
    static public void Main ()
    {
        Console.WriteLine ("Hello Mono World");
	System.Object obj = new System.Object();
	int age = MonoExt.GetObjectAge(obj);
	Console.WriteLine("Object age is {0}.",age);
	GC.Collect();
	age = MonoExt.GetObjectAge(obj);
	Console.WriteLine("Object age is {0}.",age);
	GC.Collect();
        age = MonoExt.GetObjectAge(obj);
	Console.WriteLine("Object age is {0}.",age);
	 GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
        GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
	 GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
        GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
	 GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
        GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
	 GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
        GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
	 GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
        GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
	 GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
        GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
	 GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
        GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
	 GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
        GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
	 GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
        GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
	 GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
        GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
	 GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
        GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
	 GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
        GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
	 GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
        GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
	 GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
        GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
	 GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
        GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
	 GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
        GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
	 GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
        GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
	 GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
        GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
	 GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
        GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
	 GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
        GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
	 GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
        GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
	 GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);
        GC.Collect();
        age = MonoExt.GetObjectAge(obj);
        Console.WriteLine("Object age is {0}.",age);

	if (obj != null)
        Console.WriteLine ("Non null object");
    }
}
